{
  "$schema": "http://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "existingLabName": {
      "type": "string",
      "metadata": {
        "description": "Name of an existing lab where the VM template will be created or updated."
      }
    },
    "existingVMResourceId": {
      "type": "string",
      "metadata": {
        "description": "Resource ID of an existing VM from which the VM template will be created or updated."
      }
    },
    "isVMSysPrepped": {
      "type": "bool",
      "defaultValue": true,
      "metadata": {
        "description": "If the existing VM is a Windows VM, then specifies whether the VM is sysprepped (Note: If the existing VM is NOT a Windows VM, then please specify 'false')."
      }
    },
    "imageName": {
      "type": "string",
      "metadata": {
        "description": "Name of the VM template being created or updated."
      }
    },
    "imageDescription": {
      "type": "string",
      "defaultValue": "",
      "metadata": {
        "description": "Details about the VM template being created or updated."
      }
    }
  },
  "variables": {
    "resourceName": "[concat(parameters('existingLabName'), '/', parameters('imageName'))]",
    "resourceType": "Microsoft.DevTestLab/labs/customimages"
  },
  "resources": [
    {
      "apiVersion": "2015-05-21-preview",
      "name": "[variables('resourceName')]",
      "type": "Microsoft.DevTestLab/labs/customimages",
      "properties": {
        "vm": {
          "sourceVmId": "[parameters('existingVMResourceId')]",
          "sysPrep": "[parameters('isVMSysPrepped')]"
        },
        "description": "[parameters('imageDescription')]"
      }
    }
  ],
  "outputs": {
    "vmTemplateId": {
      "type": "string",
      "value": "[resourceId(variables('resourceType'), parameters('existingLabName'), parameters('imageName'))]"
    }
  }
}
